name: Update Licenses

on:
  push:
    branches:
      - main
    paths:
      - '**.py'
      - 'pyproject.toml'

jobs:
  pre-release:
    uses: ecofuture-tech/.github/.github/workflows/reusable-update-licenses.yml@main
